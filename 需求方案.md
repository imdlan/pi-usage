# pi-usage 需求方案

## 1. 项目目标

`pi-usage` 是一个可公开发布的 Pi Coding Agent Extension，用于在 Pi 中查看 AI 服务商的模型用量、套餐配额与重置时间。

第一阶段完整支持 **Z.ai / GLM Coding Plan**；架构须能平滑扩展到 OpenAI、Anthropic、OpenRouter 等服务商。项目应轻量、可靠、安全：用量查询失败绝不能影响 Pi 的正常使用。

## 2. 范围

### 第一阶段包含

- Z.ai / GLM Coding Plan 的 usage 查询。
- Pi 命令：`/usage`、`/usage zai`、`/usage refresh`、`/usage status`。
- Pi 底部 status line 的简要配额展示。
- 自动刷新、缓存、超时、降级与错误处理。
- 通用 Provider 抽象及扩展点。
- TypeScript 严格模式、测试、README、Apache-2.0 许可证。

### 第一阶段不包含

- OpenAI、Anthropic、OpenRouter 的实际用量接口实现（仅保留接入结构）。
- 修改、购买或重置任何服务商配额。
- 自建后端、账号系统、遥测或上传用户数据。

## 3. 核心功能需求

### 3.1 命令

| 命令 | 行为 |
| --- | --- |
| `/usage` | 展示全部已配置且可用 Provider 的用量摘要（含各配额的相对重置时间，如 `in 2h 13m`，接口提供时）；无可用 Provider 时给出明确、可操作的说明。 |
| `/usage zai` | 展示 Z.ai / GLM Coding Plan 的详细用量，包括各配额、模型用量与重置时间（接口提供时）。 |
| `/usage refresh` | 忽略有效缓存并立即刷新全部可用 Provider；刷新失败时保留并展示上次成功结果及失败提示。 |
| `/usage status` | 展示当前 status line 的内容、最后成功刷新时间、缓存状态和各 Provider 可用性。 |

命令输出须适合终端阅读；不要显示 API Key、Authorization header、Cookie 或完整 HTTP 响应。

### 3.2 Status line

- 在 Pi 启动后尝试异步查询，并写入底部 status line。
- Z.ai 示例：`GLM 5.2 · 5h 32% · week 18%`。
- 数据不可用时显示：`GLM · usage unavailable`。
- 多 Provider 时保持短小；按稳定顺序展示，必要时使用汇总或截断。
- Status line 更新不得阻塞模型请求、命令处理或 Pi UI。

### 3.3 自动刷新与缓存

- 默认缓存有效期：5 分钟；配置项应集中定义，便于以后调整。
- session 启动时异步刷新一次。
- 后台定期刷新；不能因网络抖动形成重试风暴。
- 不在每次模型请求时调用 usage API。
- 并发请求同一 Provider 时应复用进行中的请求，避免重复网络调用。
- 请求失败时保留最后一次成功快照；在结果中标明数据可能过期。
- 应支持 timeout、网络错误、非 2xx、无效 JSON、字段缺失与认证失败。

## 4. Z.ai / GLM Coding Plan

### 4.1 调研优先原则

在编写接口调用前，必须研究并复用官方仓库 [zai-org/zai-coding-plugins](https://github.com/zai-org/zai-coding-plugins) 中 `glm-plan-usage` 的实现逻辑。

- 以该官方实现确认 endpoint、认证来源、请求头、返回结构与字段含义。
- 不猜测 API、不凭印象编造 endpoint 或字段。
- 若官方实现依赖 Pi 已有认证，应优先使用 Pi 已授权的 Provider/API 能力或服务商官方 usage endpoint；不得从认证文件提取、复制或解析密钥。
- 若认证信息不可安全、合法地获取，应将 Provider 标记为不可用，并在命令输出中说明下一步；不要扫描与 usage 无关的敏感文件。
- 所有请求必须使用 HTTPS。

### 4.2 展示内容

优先解析并展示（以官方实际返回为准）：

- 5-hour quota；
- weekly quota；
- 按模型的 usage；
- 配额重置时间；
- 用量百分比、剩余量或已用量（按接口可提供的最佳信息）。

## 5. 技术方案

### 5.1 技术约束

- 使用 TypeScript，开启 `strict: true`。
- 尽量零 runtime dependencies；优先使用 Pi Extension API 与 Node.js 内置能力（如 `fetch`、`AbortController`）。
- 仅在开发、测试或构建确有必要时引入 devDependencies，并在 README 说明用途。
- 所有外部数据在运行时校验；不要相信未经验证的 JSON 结构。
- 保持 ESM/模块格式与 Pi 官方 Extension 示例一致。

### 5.2 Provider abstraction

核心数据模型不得写死为 5 小时或周配额，建议如下：

```ts
export type UsageQuota = {
  id: string;
  label: string;
  used?: number;
  limit?: number;
  remaining?: number;
  percentage?: number;
  resetAt?: string;
};

export type ModelUsage = {
  model: string;
  quotas?: UsageQuota[];
  used?: number;
  limit?: number;
  percentage?: number;
};

export type UsageSnapshot = {
  provider: string;
  timestamp: string;
  quotas: UsageQuota[];
  models?: ModelUsage[];
  error?: { code: string; message: string };
  stale?: boolean;
};

export interface UsageProvider {
  readonly id: string;
  readonly name: string;
  isAvailable(): Promise<boolean>;
  getUsage(options?: { forceRefresh?: boolean }): Promise<UsageSnapshot>;
}
```

实现时可按需要细化类型，但须维持：Provider 独立、快照通用、错误可控、易于新增 Provider。

### 5.3 缓存与服务层

- 为每个 Provider 建立内存缓存，保存最后成功快照、成功时间及进行中的 Promise。
- 将网络请求、数据转换、缓存策略、Pi UI 渲染解耦。
- `forceRefresh` 只绕过有效缓存，不应绕过 timeout、错误处理与并发去重。
- 刷新流程返回结构化结果，由命令输出和 status line 共享渲染逻辑。

## 6. 项目结构建议

```text
pi-usage/
├── src/
│   ├── index.ts                 # Pi extension 注册、生命周期与命令
│   ├── providers/
│   │   ├── types.ts             # 通用 Provider/快照类型
│   │   ├── registry.ts          # Provider 注册与发现
│   │   └── zai.ts               # Z.ai 官方接口适配
│   ├── services/
│   │   ├── usage-service.ts     # 刷新、缓存、并发去重
│   │   └── status-line.ts       # status line 更新策略
│   ├── formatters/
│   │   └── usage.ts             # 命令与状态文本格式化
│   └── utils/
│       ├── http.ts              # timeout、安全错误归一化
│       └── time.ts              # 时间与百分比处理
├── test/
├── README.md
├── LICENSE
├── package.json
├── tsconfig.json
└── 需求方案.md
```

目录可根据 Pi Extension 的官方加载方式调整，但必须保持职责清晰。

## 7. 安全与隐私要求

### 7.1 最小权限与凭据边界

- 插件仅有读取 usage 数据的权限；不得修改项目文件、用户配置、服务商配额或认证状态。
- 绝不读取、解析、复制、上传或要求放宽 `~/.pi/agent/auth.json`、API Key、token、Cookie、`.env`、`~/.ssh`、`~/.aws`、`~/.gnupg`、`~/.netrc` 或其他凭据。
- 不得递归扫描用户主目录、项目目录或 Pi 配置目录来发现认证信息。
- 优先通过 Pi 已授权的 Provider/API 能力或服务商官方 usage endpoint 查询；不得从 Pi 认证文件或其他本地认证文件中提取密钥。
- 若所需授权能力不可用、无法安全调用或无法确认其来源，Provider 必须标记为不可用并给出不含敏感信息的说明；默认 fail-closed，不得尝试备用扫描、猜测凭据或降级为任意请求。
- 插件配置不得保存明文密钥、token、Cookie 或 Authorization header；如未来需要用户配置，只允许非敏感的开关、Provider 启用状态与受控 endpoint 标识。

### 7.2 网络与数据处理

- 所有网络请求必须使用 HTTPS，且仅可访问每个 Provider 明确定义、经官方实现或官方文档确认的 usage API 域名。
- 实现显式网络 allowlist；禁止用户配置任意 URL、重定向到非白名单域名，以及任何未验证的外部请求。
- 不上传、不收集、不缓存用户代码、提示词、会话内容、文件路径或其他与 usage 查询无关的数据；不实现遥测。未来若考虑遥测，必须先获得用户明确同意。
- 请求体、请求头和响应解析只保留查询 usage 所必需的字段；不得记录完整 HTTP request/response。
- 多 Provider 架构必须让每个 Provider 的认证来源、可访问域名、请求头构造及数据缓存策略显式声明且相互隔离，禁止一个 Provider 复用、读取或传递另一个 Provider 的凭据或响应数据。

### 7.3 脱敏、日志与沙箱兼容

- 禁止硬编码、打印、提交或上传 credential。
- 日志、异常消息、status line、命令输出与测试夹具中不得包含 API Key、token、Cookie、Authorization header、敏感文件路径、完整 HTTP request/response 或原始认证错误内容。
- 错误信息必须经过统一脱敏；仅保留诊断所需的 HTTP 状态、受控错误码和简短描述，且不得泄露 token 的任意片段。
- `.gitignore` 覆盖本地环境文件、测试录制数据、日志、缓存与可能的 credential 文件。
- 任何可选调试模式默认关闭；启用后也必须遵循相同的脱敏与网络白名单规则。
- 当前环境使用 `pi-secure-it`：插件必须在其 sandbox 下正常工作，不得要求放宽 `~/.pi/agent/auth.json`、`~/.ssh`、`~/.aws`、`~/.gnupg` 的读取保护，也不得依赖 shell 命令绕过限制。
- 插件运行时不得执行 shell 命令或启动子进程；只使用 Pi Extension API、受控 `fetch` 与 Node.js 内置的非 shell 能力。

## 8. 测试要求

- 单元测试：配额百分比/剩余量计算、时间格式化、状态文本截断、错误脱敏。
- Provider 测试：使用模拟 HTTP 响应覆盖正常、字段缺失、无效 JSON、401/403、429、5xx、timeout。
- 缓存测试：命中、过期、强制刷新、失败保留旧快照、并发去重。
- 命令测试：`/usage`、`/usage zai`、`/usage refresh`、`/usage status` 的路由和输出。
- 运行 TypeScript 类型检查与全部测试；不得以 `any` 绕过关键边界。
- 测试数据必须是脱敏的虚构数据，不能使用真实账户响应或密钥。
- 增加敏感信息扫描测试：覆盖源码、测试夹具、日志与构建产物，拦截疑似 API Key、token、Authorization、Cookie、私钥和 `.env` 内容。
- 增加网络 allowlist 测试：允许已声明的官方 usage API 域名，拒绝任意 URL、非 HTTPS URL、重定向到非白名单域名及恶意 endpoint 配置。
- 增加日志与 UI 脱敏测试：将模拟 token、Cookie 和认证错误注入所有错误路径，断言命令输出、status line 与日志均不含敏感片段。
- 增加恶意配置与权限边界测试：拒绝明文密钥配置、跨 Provider 凭据引用、敏感文件路径配置及任何触发 shell/subprocess 的实现；授权能力不可用时应安全失败且不发起越权请求。

## 9. 文档与开源要求

- 提供英文 `README.md`：项目目的、安装方式、Pi Extension 加载方式、命令示例、支持范围、隐私与安全说明、开发与测试命令、贡献方式。
- 添加 Apache-2.0 `LICENSE`。
- README 明确声明：Z.ai 集成以官方 `glm-plan-usage` 行为为依据；接口可能变化；扩展不保存或上传密钥。
- 不提交 credential、私有配置、真实响应样本或 lockfile 中的秘密信息。

## 10. 验收标准

1. Pi 能加载 Extension，且失败不会影响 Pi 正常运行。
2. `/usage`、`/usage zai`、`/usage refresh`、`/usage status` 均可用且输出可读。
3. Z.ai 实现的接口与认证逻辑可追溯到 `zai-org/zai-coding-plugins` 的 `glm-plan-usage`，没有猜测接口。
4. 可用时显示 5-hour、weekly、模型用量和重置时间中的官方可提供项目。
5. Status line 在启动和后台刷新后更新；失败时显示降级状态。
6. 缓存默认约 5 分钟，强制刷新有效，并发调用不重复发起同一请求。
7. 请求异常、认证失败和返回格式变化都有安全、可理解的处理。
8. 插件在 `pi-secure-it` sandbox 下可正常查询已授权的 usage 数据，且不要求放宽 `~/.pi/agent/auth.json`、`~/.ssh`、`~/.aws`、`~/.gnupg` 的保护。
9. 网络请求仅限经确认的官方 usage API allowlist；配置、日志、UI、错误和测试产物均不泄露敏感信息。
10. `tsc` 严格类型检查及全部测试通过，包括敏感信息扫描、网络 allowlist、日志脱敏和恶意配置测试。
11. README、Apache-2.0 LICENSE、`.gitignore` 完整。
12. Git 工作区不含任何 credential，且本次开发不执行 `git push`。

## 11. 建议开发步骤

1. 阅读 Pi Extension 官方文档与 Z.ai 官方 `glm-plan-usage` 源码，记录已确认的接口事实及链接。
2. 初始化 TypeScript strict 项目与最小 Pi Extension 入口，确认能被 Pi 加载。
3. 建立类型、Provider registry、缓存服务与格式化器，并先为纯逻辑写测试。
4. 实现 Z.ai adapter：仅使用已授权 Provider/API 能力或官方 usage endpoint，认证获取最小化、HTTPS allowlist、timeout、响应校验与脱敏错误；不得读取认证文件或执行 shell 命令。
5. 接入四个 `/usage` 子命令和 status line；验证启动、定时刷新及失败降级。
6. 完善测试、README、LICENSE、`.gitignore`，加入敏感信息扫描、网络 allowlist、日志脱敏和恶意配置测试，并运行类型检查和测试。
7. 检查 `git diff`、`git status` 与潜在敏感信息；确认可在 `pi-secure-it` sandbox 下运行且无需放宽保护；仅保留本地提交准备，不执行 push。

## 12. 注意事项

- 官方接口与返回字段可能变动：将 API 解析集中在 `zai.ts`，避免扩散到 UI 层。
- 不要从模型调用路径同步查询用量；这会损害响应速度并可能触发限流。
- status line 空间有限：优先显示最关键的两类配额，详细信息交给命令输出。
- 日期时间应保留原始 ISO 值并明确时区展示，避免把 reset time 误解为本地时间。
- 若只有百分比或只有 remaining，格式化层应优雅降级，不能臆造 used/limit。
- 新增 Provider 时必须独立实现认证和测试，不能假定其配额窗口或数据模型与 Z.ai 相同。
- 每个新增 Provider 都必须单独声明认证访问策略、网络 allowlist、脱敏规则与失败边界，不能共享或回退到其他 Provider 的凭据。

### 给 Pi 开发时的安全硬约束

```text
1. 只读 usage 数据；不修改项目文件、Pi 配置、认证状态或服务商配额。
2. 不读取、不解析、不上传 ~/.pi/agent/auth.json、.env、API Key、token、Cookie、~/.ssh、~/.aws、~/.gnupg、~/.netrc 或其他凭据。
3. 不扫描主目录、项目目录或 Pi 配置目录来寻找凭据；不执行 shell 命令或子进程。
4. 仅使用 Pi 已授权的 Provider/API 能力或经官方确认的 HTTPS usage endpoint；认证不可安全获取时直接 fail-closed。
5. 网络严格使用 Provider 独立 allowlist；禁止任意 URL、非 HTTPS、越权重定向和跨 Provider 凭据访问。
6. 配置不保存明文密钥；日志、命令输出、status line、异常和测试数据必须脱敏，绝不显示 token 片段或完整 HTTP 内容。
7. 不收集、不上传用户代码、提示词、会话内容或无关元数据；不实现遥测。
8. 必须在 pi-secure-it sandbox 下工作，绝不要求放宽 ~/.pi/agent/auth.json、~/.ssh、~/.aws、~/.gnupg 的保护。
9. 安全检查失败时默认拒绝操作，并覆盖敏感信息扫描、allowlist、脱敏和恶意配置测试。
```

## 13. 可直接交给 Pi 的开发提示词

```text
请在当前仓库实现需求方案.md中定义的 pi-usage Pi Coding Agent Extension。

先做调研：阅读 Pi Extension 官方文档，以及 https://github.com/zai-org/zai-coding-plugins 中 glm-plan-usage 的真实实现。确认 Z.ai 的 endpoint、认证来源、请求头和返回字段；不要猜 API，也不要编造字段。将确认依据记录在 README 或代码注释中（不得复制或泄露任何 credential）。

随后实现：第一阶段完整支持 Z.ai / GLM Coding Plan，并预留 OpenAI、Anthropic、OpenRouter 的 Provider 接入结构。实现 /usage、/usage zai、/usage refresh、/usage status 以及非阻塞 status line。默认缓存约 5 分钟，启动异步刷新、后台定期刷新、请求去重；刷新失败保留上次成功数据并标记过期。所有网络请求使用 HTTPS 和 Provider 独立的官方 usage API allowlist，并处理 timeout、非 2xx、无效 JSON、字段缺失和认证失败。只能使用 Pi 已授权的 Provider/API 能力或经官方确认的 usage endpoint；不得从 `~/.pi/agent/auth.json`、`.env`、API Key、token、`~/.ssh`、`~/.aws`、`~/.gnupg` 或其他本地认证文件读取、解析或提取凭据。认证无法安全获取时必须 fail-closed。

使用 TypeScript strict，尽量零 runtime dependencies，优先使用 Node 内置 fetch 与 Pi 官方 Extension API。使用通用 UsageProvider、UsageSnapshot、UsageQuota 模型，不要把 5-hour 或 weekly 写死到核心架构。网络、缓存、Provider、格式化和 Pi UI 要分层。

补齐单元测试和模拟 Provider 测试：正常响应、字段缺失、无效 JSON、401/403、429、5xx、timeout、缓存命中/过期/强制刷新/失败回退/并发去重，以及命令路由。补充敏感信息扫描、网络 allowlist、日志和 UI 脱敏、恶意配置与跨 Provider 隔离测试。运行类型检查和全部测试。

添加英文 README、Apache-2.0 LICENSE、.gitignore。严禁输出、记录、提交任何 API Key、token、Cookie、Authorization header 或完整 HTTP response；不得扫描无关敏感文件、不得执行 shell 命令、不得收集用户代码或会话内容。插件必须在 pi-secure-it sandbox 下正常工作，绝不要求放宽 `~/.pi/agent/auth.json`、`~/.ssh`、`~/.aws`、`~/.gnupg` 的保护。完成后检查 git diff 和 git status，但不要执行 git push。请简洁报告实现内容、验证结果、未能确认的官方接口事实与后续建议。
```
